import React from 'react';

const Display = (props) => {

    //props
    const message = props.message;

    //state

    //function

    //style

    //execution
    return (
        <>
            <div>
                <label>{message}</label>
            </div>
        </>
    );
}

export default Display;